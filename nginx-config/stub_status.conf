# Exposes NGINX stub_status metrics on an internal port (not published to the
# host) so the nginx-prometheus-exporter can scrape request volume/connections.
server {
    listen 8081;

    location /stub_status {
        stub_status;
    }
}
